# Salman Faris A V Portfolio

A dependency-free static portfolio built from resume content, using the Figma file only as a visual direction reference.

## Run locally

Open `index.html` directly, or serve the folder:

```bash
python3 -m http.server 4173
```

Then visit `http://127.0.0.1:4173`.
